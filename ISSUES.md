# Open issues

Known gaps to revisit.

## Cowork dashboard-persistence claim not yet verified end-to-end

`SKILL.md`/`README.md` now state that a Cowork-produced dashboard Artifact persists to the user's Artifacts library, grounded in direct, hands-on testing of the platform itself. Not yet confirmed that this skill's own `inject_data()` → Artifact flow reproduces that when run in a genuinely fresh Cowork session – see the planned Cowork blind test in `TESTING.md`.
