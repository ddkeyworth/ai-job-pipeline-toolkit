# Preferences

Example `preferences.md` for the fictional Jordan Ainsley persona (see `CV-example.md`) – demonstrates the file format documented in `schema/SCHEMA.md` → "Preferences file". Collected as an explicit question, not read off the CV.

## Compensation floor
On-target earnings (base salary + target bonus/variable comp; equity explicitly excluded – see `config/weights.json` → `components.comp.equity_handling`): £110,000

Confirmed: 2026-04-01
